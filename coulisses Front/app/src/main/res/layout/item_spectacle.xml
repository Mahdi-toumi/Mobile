<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageView
            android:id="@+id/spectacleImage"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/spectacleTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textColor="@color/text_primary"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/spectacleImage"
            app:layout_constraintTop_toTopOf="@id/spectacleImage" />

        <TextView
            android:id="@+id/spectacleDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/text_secondary"
            app:layout_constraintEnd_toEndOf="@id/spectacleTitle"
            app:layout_constraintStart_toStartOf="@id/spectacleTitle"
            app:layout_constraintTop_toBottomOf="@id/spectacleTitle" />

        <TextView
            android:id="@+id/spectacleLocation"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/text_secondary"
            app:layout_constraintEnd_toEndOf="@id/spectacleDate"
            app:layout_constraintStart_toStartOf="@id/spectacleDate"
            app:layout_constraintTop_toBottomOf="@id/spectacleDate" />

        <TextView
            android:id="@+id/spectacleTime"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/text_secondary"
            app:layout_constraintEnd_toEndOf="@id/spectacleLocation"
            app:layout_constraintStart_toStartOf="@id/spectacleLocation"
            app:layout_constraintTop_toBottomOf="@id/spectacleLocation" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>